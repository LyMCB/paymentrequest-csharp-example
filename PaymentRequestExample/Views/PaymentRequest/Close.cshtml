@model PaymentRequestExample.PaymentRequest.listpaymentrequestresponse

@{
    ViewBag.Title = "Close";
}

<h1>Payment Request  <small>Close</small></h1>
@if (Model.paymentrequestlist != null)
{

    <table class="table table-hover table-striped">
        <thead>
            <tr>
                <th>Payment Request ID</th>
                <th>Amount</th>
                <th>Created</th>
                <th>Currency</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.paymentrequestlist)
            {
            <tr>
                <td>@item.paymentrequestid</td>
                <td>@item.amount formid</td>
                <td>@item.created</td>
                <td>@item.currency</td>
                <td>
                    <div class="btn-group pull-right">
                        <a class="btn dropdown-toggle" data-toggle="dropdown" href="#">Action <span class="caret"></span>
                        </a>
                        <ul class="dropdown-menu">
                            <li>@Html.ActionLink("Close", "ClosePaymentRequest", "paymentrequest", new { paymentRequestId = item.paymentrequestid }, null)</li>
                        </ul>
                    </div>
                </td>
            </tr>
            }

        </tbody>
    </table>
            
}

else
{
    <div class="alert warning">
        <button class="close" data-dismiss="alert">×</button>
        <strong>Note!</strong>
        No forms found
    </div> 
}
